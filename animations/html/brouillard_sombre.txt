<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    /* Fond Nuit Profonde */
    --bg-color: #0a0b10;
    /* Brouillard gris-bleu (Nappe vaporeuse) */
    --fog-color: rgba(90, 105, 130, 0.4);
    /* Lueur de profondeur (Base du brouillard) */
    --fog-glow: rgba(60, 80, 110, 0.3);
    --text-main: #ffffff;
  }

  body {
    background: #1a1b26;
    display: flex; justify-content: center; align-items: center;
    height: 100vh; margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px; height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', system-ui, sans-serif;
    box-shadow: 0 20px 50px rgba(0,0,0,0.5);
  }

  /* --- NAPPES DE BROUILLARD NOCTURNE --- */
  .fog-container {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .fog-layer {
    position: absolute;
    bottom: -60px;
    left: -100px;
    right: -100px;
    height: 250px;
    border-radius: 50%;
    background: var(--fog-color);
    filter: blur(60px); /* On garde votre flou massif favori */
    animation: fog-breathe 12s infinite alternate ease-in-out;
  }

  /* Dégradé de base pour asseoir le brouillard au sol */
  .fog-depth {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, var(--fog-glow) 0%, transparent 70%);
    z-index: 2;
  }

  @keyframes fog-breathe {
    0% { transform: translateY(0) scale(1); opacity: 0.5; }
    100% { transform: translateY(-40px) scale(1.15); opacity: 0.8; }
  }

  /* --- INTERFACE --- */
  .ui-content {
    position: relative; z-index: 10;
    color: var(--text-main); text-align: center; padding-top: 50px;
  }
  .temp { font-size: 72px; font-weight: 900; margin: 0; letter-spacing: -3px; }
  .condition { font-size: 22px; font-weight: 500; opacity: 0.7; margin-top: -8px; }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="fog-depth"></div>

  <div class="fog-container">
    <div class="fog-layer" style="animation-delay: 0s; height: 220px;"></div>
    <div class="fog-layer" style="animation-delay: -5s; height: 170px; opacity: 0.4;"></div>
    <div class="fog-layer" style="animation-delay: -9s; height: 240px; width: 130%; left: -15%;"></div>
  </div>

  <div class="ui-content">
    <p class="temp">4°C</p>
    <p class="condition">Brume Nocturne</p>
  </div>
</div>

</body>
</html>
