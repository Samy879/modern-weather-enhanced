<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    /* Fond "Ciel de Jour" légèrement plus coloré pour le contraste */
    --bg-color: #d6e0ea;
    --cloud-light: rgba(255, 255, 255, 0.8);
    --cloud-dark: rgba(148, 163, 184, 0.4); /* Gris-bleu pour donner du volume */
    --text-main: #2e3136;
  }

  body {
    background: #c0c0c0;
    display: flex; justify-content: center; align-items: center;
    height: 100vh; margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px; height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
  }

  /* --- CONTENEUR DES NUAGES --- */
  .clouds-container {
    position: absolute;
    inset: -100px; /* On dépasse pour éviter les bords coupés */
    z-index: 1;
    pointer-events: none;
  }

  .cloud {
    position: absolute;
    border-radius: 50%;
    filter: blur(35px); /* Flou réduit pour plus de présence */
    animation: drift linear infinite;
  }

  /* Types de nuages pour créer de la profondeur */
  .cloud-light { background: var(--cloud-light); }
  .cloud-shadow { background: var(--cloud-dark); }

  /* Tailles */
  .large { width: 350px; height: 180px; }
  .medium { width: 250px; height: 120px; }
  .small { width: 150px; height: 80px; }

  @keyframes drift {
    from { transform: translateX(-100%); }
    to { transform: translateX(200%); }
  }

  /* --- INTERFACE --- */
  .ui-content {
    position: relative; z-index: 10;
    color: var(--text-main); text-align: center; padding-top: 50px;
    text-shadow: 0 0 10px rgba(255,255,255,0.5);
  }
  .temp { font-size: 68px; font-weight: 800; margin: 0; letter-spacing: -2px; }
  .condition { font-size: 20px; opacity: 0.8; margin-top: -5px; }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="clouds-container">
    <div class="cloud cloud-shadow large" style="top: 15%; left: -20%; animation-duration: 50s; animation-delay: -5s;"></div>
    <div class="cloud cloud-shadow medium" style="top: 45%; left: -10%; animation-duration: 65s; animation-delay: -20s;"></div>

    <div class="cloud cloud-light large" style="top: 5%; left: -40%; animation-duration: 40s;"></div>
    <div class="cloud cloud-light medium" style="top: 35%; left: -30%; animation-duration: 55s; animation-delay: -10s;"></div>
    <div class="cloud cloud-light small" style="top: 60%; left: -15%; animation-duration: 30s;"></div>
  </div>

  <div class="ui-content">
    <p class="temp">18.5°C</p>
    <p class="condition">Ciel Couvert</p>
  </div>
</div>

</body>
</html>
