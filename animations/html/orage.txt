<!DOCTYPE html>
<html>
<head>
<style>
  :root {
    --bg-color: #0d0e17;
    --fog-color: rgba(110, 130, 170, 0.35);
    /* Les différents niveaux d'intensité */
    --bolt-near: rgba(255, 255, 255, 0.45); /* Proche, brillant mais doux */
    --bolt-std: rgba(255, 255, 255, 0.3);  /* Standard */
    --bolt-low: rgba(255, 255, 255, 0.12); /* NOUVEAU : Lointain, très faible */
  }

  .widget-preview {
    position: relative;
    width: 350px; height: 300px;
    background: var(--bg-color);
    border-radius: 12px;
    margin: 50px auto;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
  }

  /* --- BROUILLARD --- */
  .fog-system {
    position: absolute;
    inset: -60px;
    filter: blur(80px);
    opacity: 0.7;
    z-index: 1;
  }
  .fog-nappe {
    position: absolute;
    background: radial-gradient(circle, var(--fog-color) 0%, transparent 75%);
    border-radius: 50%;
    animation: drift 30s infinite alternate ease-in-out;
  }
  @keyframes drift { from { transform: translate(-10%, -10%) scale(1); } to { transform: translate(15%, 15%) scale(1.4); } }

  /* --- ÉCLAIRS --- */
  .lightning {
    position: absolute;
    opacity: 0;
    border-radius: 50%;
    z-index: 5;
    filter: blur(60px);
  }

  /* --- Types existants (Salves nerveuses) --- */
  .bolt-tl { width: 450px; height: 450px; top: -180px; left: -180px; background: radial-gradient(circle, var(--bolt-std) 0%, transparent 80%); animation: single-nerve 14s infinite; }
  .bolt-tr { width: 500px; height: 400px; top: -150px; right: -100px; background: radial-gradient(circle, var(--bolt-std) 0%, transparent 80%); animation: double-nerve-jitter 22s infinite 4s; }
  .bolt-near-br { width: 600px; height: 600px; bottom: -200px; right: -150px; background: radial-gradient(circle, var(--bolt-near) 0%, transparent 80%); filter: blur(45px); animation: single-nerve 17s infinite 2s; }
  .bolt-bl { width: 400px; height: 400px; bottom: -100px; left: -100px; background: radial-gradient(circle, var(--bolt-std) 0%, transparent 80%); animation: triple-nerve-jitter 26s infinite 8s; }

  /* --- NOUVEAUX TYPES (Lents et faibles) --- */

  /* 5. Lointain Haut (Unique, lent) */
  .bolt-far-top {
    width: 700px; height: 400px; top: -300px; left: -100px;
    background: radial-gradient(circle, var(--bolt-low) 0%, transparent 85%);
    filter: blur(80px); /* Très flou pour la distance */
    animation: slow-single-pulse 30s infinite 1s;
  }

  /* 6. Lointain Bas (Unique, très lent) */
  .bolt-far-bottom {
    width: 600px; height: 500px; bottom: -350px; left: 50px;
    background: radial-gradient(circle, var(--bolt-low) 0%, transparent 85%);
    filter: blur(80px);
    animation: slow-single-pulse 37s infinite 15s;
  }


  /* --- ANIMATIONS --- */

  /* NOUVELLE ANIMATION : Pulsation unique et lente */
  @keyframes slow-single-pulse {
    0%, 15%, 85%, 100% { opacity: 0; transform: scale(0.95); }
    /* Montée lente */
    40% { opacity: 1; transform: scale(1.1); }
    /* Descente lente */
    60% { opacity: 0.5; transform: scale(1.05); }
  }

  /* Animations nerveuses existantes */
  @keyframes single-nerve {
    0%, 12%, 20%, 100% { opacity: 0; transform: scale(0.95); } 13% { opacity: 1; transform: scale(1.05); } 13.5% { opacity: 0.2; } 14.5% { opacity: 0.8; transform: scale(1.1); }
  }
  @keyframes double-nerve-jitter {
    0%, 8%, 25%, 100% { opacity: 0; transform: scale(0.95) translate(0,0); } 10% { opacity: 0.8; transform: scale(1) translate(0,0); } 11.5% { opacity: 0.2; } 16% { opacity: 0.9; transform: scale(1.1) translate(20px, -10px); } 17.5% { opacity: 0.6; }
  }
  @keyframes triple-nerve-jitter {
    0%, 5%, 25%, 100% { opacity: 0; transform: translate(0,0); } 6% { opacity: 0.7; transform: scale(1) translate(0,0); } 7% { opacity: 0; } 9% { opacity: 0.8; transform: scale(1.05) translate(15px, 20px); } 10.5% { opacity: 0.7; } 12% { opacity: 0; } 15% { opacity: 1; transform: scale(1.1) translate(-20px, -15px); } 16.5% { opacity: 0.8; }
  }

  .ui-content { position: relative; z-index: 10; color: white; text-align: center; padding-top: 40px; }
  .temp { font-size: 64px; font-weight: bold; margin: 0; }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="fog-system">
    <div class="fog-nappe" style="width: 500px; height: 350px; top: 0; left: 0;"></div>
    <div class="fog-nappe" style="width: 400px; height: 400px; bottom: 0; right: 0; animation-delay: -10s;"></div>
  </div>

  <div class="lightning bolt-tl"></div>
  <div class="lightning bolt-tr"></div>
  <div class="lightning bolt-near-br"></div>
  <div class="lightning bolt-bl"></div>

  <div class="lightning bolt-far-top"></div>
  <div class="lightning bolt-far-bottom"></div>

  <div class="ui-content">
    <p class="temp">8.8°C</p>
    <p style="font-size: 22px; opacity: 0.8; margin: 0;">Orage Violent</p>
  </div>
</div>

</body>
</html>
