<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    /* Thème Sombre : Un soleil couchant ou une aube chaleureuse */
    --bg-color: #0d0e17;
    --sun-core: rgba(255, 200, 80, 0.2);
    --sun-ray: rgba(255, 220, 150, 0.05);
    --text-main: #ffffff;
  }

  /* Thème CLAIR (Décommentez pour tester)
    --bg-color: #fdf9f0;
    --sun-core: rgba(255, 180, 0, 0.15);
    --sun-ray: rgba(255, 150, 0, 0.08);
    --text-main: #5c4a33;
  */

  body {
    background: #1a1b26;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px;
    height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
    box-shadow: 0 25px 50px rgba(0,0,0,0.3);
  }

  /* --- LUEUR CENTRALE (Cœur du soleil) --- */
  .sun-glow {
    position: absolute;
    top: -50px;
    right: -50px;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, var(--sun-core) 0%, transparent 70%);
    filter: blur(40px);
    z-index: 1;
    animation: pulse-glow 10s infinite alternate ease-in-out;
  }

  @keyframes pulse-glow {
    from { opacity: 0.6; transform: scale(1); }
    to { opacity: 1; transform: scale(1.1); }
  }

  /* --- RAYONS (God Rays) --- */
  .rays-container {
    position: absolute;
    top: -150px;
    right: -150px;
    width: 600px;
    height: 600px;
    z-index: 2;
    animation: slow-rotate 120s infinite linear; /* Rotation quasi-imperceptible */
    opacity: 0.8;
  }

  .ray {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 800px; /* Très longs rayons */
    height: 60px;
    background: conic-gradient(from 0deg at 0% 50%, var(--sun-ray), transparent 30%);
    transform-origin: 0% 50%;
    filter: blur(20px);
  }

  /* --- INTERFACE --- */
  .ui-content {
    position: relative;
    z-index: 10;
    color: var(--text-main);
    text-align: center;
    padding-top: 50px;
  }

  .temp { font-size: 68px; font-weight: 800; margin: 0; letter-spacing: -2px; }
  .condition { font-size: 20px; opacity: 0.8; margin-top: -5px; }

  /* Rotation lente pour le réalisme */
  @keyframes slow-rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="sun-glow"></div>

  <div class="rays-container">
    <div class="ray" style="transform: rotate(0deg);"></div>
    <div class="ray" style="transform: rotate(45deg);"></div>
    <div class="ray" style="transform: rotate(90deg);"></div>
    <div class="ray" style="transform: rotate(135deg);"></div>
    <div class="ray" style="transform: rotate(180deg);"></div>
    <div class="ray" style="transform: rotate(225deg);"></div>
    <div class="ray" style="transform: rotate(270deg);"></div>
    <div class="ray" style="transform: rotate(315deg);"></div>
  </div>

  <div class="ui-content">
    <p class="temp">24.5°C</p>
    <p class="condition">Ensoleillé</p>
  </div>
</div>

</body>
</html>
