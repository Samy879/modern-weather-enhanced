<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    --bg-color: #0d0e17;
    --streak-color: rgba(255, 255, 255, 0.4);
    --drift-color: rgba(110, 130, 170, 0.2);
    --text-main: #ffffff;
  }

  /* Version CLAIRE (optionnelle)
    --bg-color: #e9edf1;
    --streak-color: rgba(60, 80, 120, 0.25);
    --drift-color: rgba(70, 90, 130, 0.15);
    --text-main: #4a4d5e;
  */

  body {
    background: #1a1b26;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px;
    height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
    box-shadow: 0 25px 50px rgba(0,0,0,0.3);
  }

  /* --- FOND VAPOREUX --- */
  .wind-drift {
    position: absolute;
    inset: -60px;
    background: radial-gradient(circle at 15% 50%, var(--drift-color) 0%, transparent 70%);
    filter: blur(50px);
    animation: drift-bg 18s infinite alternate ease-in-out;
    z-index: 1;
  }

  @keyframes drift-bg {
    from { transform: translateX(-15%); opacity: 0.3; }
    to { transform: translateX(18%); opacity: 0.7; }
  }

  /* --- TRAINÉES DE VENT AFFINÉES --- */
  .streak {
    position: absolute;
    height: 1.5px; /* Épaisseur réduite pour plus de finesse */
    background: linear-gradient(90deg,
      transparent 0%,
      var(--streak-color) 20%,
      var(--streak-color) 80%,
      transparent 100%);
    filter: blur(0.8px); /* Flou légèrement réduit pour rester net à 1.5px */
    border-radius: 2px;
    opacity: 0;
    z-index: 2;
  }

  .streak-fast { width: 180px; animation: wind-sweep 2.2s infinite linear; }
  .streak-slow { width: 120px; animation: wind-sweep 4.2s infinite linear; }
  .streak-gust { width: 280px; animation: wind-sweep 1.6s infinite cubic-bezier(0.4, 0, 0.2, 1); }

  @keyframes wind-sweep {
    0% { transform: translateX(-350px); opacity: 0; }
    20% { opacity: 0.6; }
    80% { opacity: 0.6; }
    100% { transform: translateX(550px); opacity: 0; }
  }

  .ui-content {
    position: relative;
    z-index: 10;
    color: var(--text-main);
    text-align: center;
    padding-top: 50px;
  }

  .temp { font-size: 68px; font-weight: 800; margin: 0; letter-spacing: -2px; }
  .condition { font-size: 20px; opacity: 0.8; margin-top: -5px; }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="wind-drift"></div>

  <div class="streak streak-fast" style="top: 20%; animation-delay: 0.2s;"></div>
  <div class="streak streak-fast" style="top: 23%; animation-delay: 0.35s; height: 0.8px; opacity: 0.2;"></div>

  <div class="streak streak-gust" style="top: 48%; animation-delay: 1.5s;"></div>

  <div class="streak streak-slow" style="top: 72%; animation-delay: 0.8s;"></div>
  <div class="streak streak-slow" style="top: 75%; animation-delay: 1.1s; height: 0.8px;"></div>

  <div class="streak streak-fast" style="top: 12%; animation-delay: 3.2s;"></div>
  <div class="streak streak-gust" style="top: 82%; animation-delay: 2.8s; width: 320px;"></div>

  <div class="ui-content">
    <p class="temp">8.8°C</p>
    <p class="condition">Vent Modéré</p>
  </div>
</div>

</body>
</html>
