<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    /* Fond contrasté validé */
    --bg-color: #e0e7f0;
    --flake-color: #ffffff;
    /* Halo de contour pour la visibilité sans l'effet "boule" 3D */
    --flake-glow: 0 0 2px rgba(100, 120, 150, 0.4);
    --cold-glow: rgba(100, 140, 220, 0.2);
    --text-main: #2e3136;
    .flake.large  { width: 5px;   height: 5px;   filter: blur(2.5px); }
    .flake.medium { width: 3.5px; height: 3.5px; filter: blur(1.5px); }
    .flake.small  { width: 2px;   height: 2px;   filter: blur(0.5px); }

  /* Halo de visibilité spécifique au thème clair pour garder l'aspect plat */
    .theme-light .flake {
    box-shadow: 0 0 2px rgba(100, 120, 150, 0.4); }
  }

  body {
    background: #d0d0d0;
    display: flex; justify-content: center; align-items: center;
    height: 100vh; margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px; height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
    box-shadow: 0 15px 35px rgba(0,0,0,0.1);
  }

  .cold-mist {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% 110%, var(--cold-glow) 0%, transparent 60%);
    z-index: 1;
  }

  .snow-container {
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
  }

  .flake {
    position: absolute;
    top: -20px;
    background: var(--flake-color);
    border-radius: 50%;
    box-shadow: var(--flake-glow);
    opacity: 0;
  }

  /* --- TAILLES ET FLOU (Repris de votre version préférée) --- */
  .flake.large { width: 5px; height: 5px; filter: blur(2.5px); }
  .flake.medium { width: 3.5px; height: 3.5px; filter: blur(1.5px); }
  .flake.small { width: 2px; height: 2px; filter: blur(0.5px); }

  /* --- VOS ANIMATIONS (Chemins A & B avec courant d'air) --- */
  @keyframes fall-path-A {
    0% { opacity: 0; transform: translate(0, 0); }
    10% { opacity: 1; transform: translate(-2px, -5px); } /* Courant d'air */
    25% { transform: translate(5px, 40px); }
    50% { transform: translate(-3px, 120px); }
    75% { transform: translate(8px, 220px); }
    100% { opacity: 0.4; transform: translate(2px, 350px); }
  }

  @keyframes fall-path-B {
    0% { opacity: 0; transform: translate(0, 0); }
    15% { opacity: 1; transform: translate(3px, -8px); } /* Courant d'air */
    35% { transform: translate(-10px, 60px); }
    65% { transform: translate(5px, 180px); }
    100% { opacity: 0.4; transform: translate(-8px, 360px); }
  }

  .ui-content {
    position: relative; z-index: 10;
    color: var(--text-main); text-align: center; padding-top: 50px;
  }
  .temp { font-size: 68px; font-weight: 800; margin: 0; letter-spacing: -2px; }
  .condition { font-size: 20px; opacity: 0.6; margin-top: -5px; }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="cold-mist"></div>

  <div class="snow-container">
    <div class="flake large" style="left: 20%; animation: fall-path-A 5s infinite linear; animation-delay: 0.5s;"></div>
    <div class="flake large" style="left: 60%; animation: fall-path-B 5.5s infinite linear; animation-delay: 2.5s;"></div>
    <div class="flake large" style="left: 85%; animation: fall-path-A 4.8s infinite linear; animation-delay: 1.2s;"></div>

    <div class="flake medium" style="left: 10%; animation: fall-path-B 7s infinite linear; animation-delay: 1s;"></div>
    <div class="flake medium" style="left: 45%; animation: fall-path-B 7.5s infinite linear; animation-delay: 4s;"></div>
    <div class="flake medium" style="left: 70%; animation: fall-path-A 6.8s infinite linear; animation-delay: 3s;"></div>
    <div class="flake medium" style="left: 30%; animation: fall-path-B 8.2s infinite linear; animation-delay: 0.2s;"></div>

    <div class="flake small" style="left: 5%;  animation: fall-path-A 10s infinite linear; animation-delay: 5s;"></div>
    <div class="flake small" style="left: 35%; animation: fall-path-A 11s infinite linear; animation-delay: 2s;"></div>
    <div class="flake small" style="left: 55%; animation: fall-path-B 9.5s infinite linear; animation-delay: 0.8s;"></div>
    <div class="flake small" style="left: 75%; animation: fall-path-B 12s infinite linear; animation-delay: 1.5s;"></div>
    <div class="flake small" style="left: 95%; animation: fall-path-B 13s infinite linear; animation-delay: 3s;"></div>
    <div class="flake small" style="left: 50%; animation: fall-path-A 11.5s infinite linear; animation-delay: 6s;"></div>
  </div>

  <div class="ui-content">
    <p class="temp">-2.4°C</p>
    <p class="condition">Neige modérée</p>
  </div>
</div>

</body>
</html>
