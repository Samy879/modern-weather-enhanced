<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    /* Fond Clair type KDE Breeze */
    --bg-color: #f6f8fa;
    --text-main: #2e3136;

    /* COULEURS AJUSTÉES POUR LE THÈME CLAIR */
    /* La grande lueur atmosphérique (Chaude et douce) */
    --sun-glow-core: rgba(255, 160, 60, 0.35); /* Cœur orangé diffus */
    --sun-glow-halo: rgba(255, 210, 100, 0.15); /* Halo doré */

    /* Les faisceaux (nets et dorés) */
    --sun-ray: rgba(255, 180, 40, 0.18);
  }

  body {
    background: #e0e0e0;
    display: flex; justify-content: center; align-items: center;
    height: 100vh; margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px; height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
    box-shadow: 0 10px 30px rgba(0,0,0,0.05);
  }

  /* --- 1. LA GRANDE LUEUR ATMOSPHÉRIQUE (Restaurée) --- */
  .sun-glow {
    position: absolute;
    top: -100px; right: -100px;
    width: 450px; height: 450px;
    /* Dégradé radial doux pour l'effet vaporeux */
    background: radial-gradient(circle,
      var(--sun-glow-core) 0%,
      var(--sun-glow-halo) 30%,
      transparent 70%);
    filter: blur(50px); /* Grand flou pour l'ambiance */
    z-index: 1;
    animation: pulse-glow 12s infinite alternate ease-in-out;
  }

  /* --- 2. LES FAISCEAUX LUMINEUX (Serrés et nets au-dessus de la lueur) --- */
  .rays-container {
    position: absolute;
    top: -150px; right: -150px;
    width: 600px; height: 600px;
    z-index: 2;
    animation: slow-rotate 120s infinite linear;
  }

  .ray {
    position: absolute;
    top: 50%; left: 50%;
    width: 800px;
    height: 40px;
    background: conic-gradient(from 0deg at 0% 50%, var(--sun-ray) 0%, transparent 12%);
    transform-origin: 0% 50%;
    filter: blur(10px); /* Traits relativement nets */
  }

  /* --- INTERFACE --- */
  .ui-content {
    position: relative; z-index: 10;
    color: var(--text-main); text-align: center; padding-top: 50px;
  }
  .temp { font-size: 68px; font-weight: 800; margin: 0; letter-spacing: -2px; }
  .condition { font-size: 20px; opacity: 0.7; margin-top: -5px; }

  @keyframes pulse-glow {
    from { opacity: 0.7; transform: scale(1); }
    to { opacity: 1; transform: scale(1.1); }
  }

  @keyframes slow-rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="sun-glow"></div>

  <div class="rays-container">
    <div class="ray" style="transform: rotate(0deg);"></div>
    <div class="ray" style="transform: rotate(22.5deg);"></div>
    <div class="ray" style="transform: rotate(45deg);"></div>
    <div class="ray" style="transform: rotate(67.5deg);"></div>
    <div class="ray" style="transform: rotate(90deg);"></div>
    <div class="ray" style="transform: rotate(112.5deg);"></div>
    <div class="ray" style="transform: rotate(135deg);"></div>
    <div class="ray" style="transform: rotate(157.5deg);"></div>
    <div class="ray" style="transform: rotate(180deg);"></div>
    <div class="ray" style="transform: rotate(202.5deg);"></div>
    <div class="ray" style="transform: rotate(225deg);"></div>
    <div class="ray" style="transform: rotate(247.5deg);"></div>
    <div class="ray" style="transform: rotate(270deg);"></div>
    <div class="ray" style="transform: rotate(292.5deg);"></div>
    <div class="ray" style="transform: rotate(315deg);"></div>
    <div class="ray" style="transform: rotate(337.5deg);"></div>
  </div>

  <div class="ui-content">
    <p class="temp">24.5°C</p>
    <p class="condition">Ensoleillé</p>
  </div>
</div>

</body>
</html>
