<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
  :root {
    /* Fond Nuit d'Encre */
    --bg-color: #05070a;
    --star-color: #ffffff;
    --moon-core: rgba(255, 255, 240, 0.8);
    --moon-halo: rgba(200, 220, 255, 0.2);
    --text-main: #ffffff;
  }

  body {
    background: #1a1b26;
    display: flex; justify-content: center; align-items: center;
    height: 100vh; margin: 0;
  }

  .widget-preview {
    position: relative;
    width: 350px; height: 300px;
    background: var(--bg-color);
    border-radius: 16px;
    overflow: hidden;
    font-family: 'Segoe UI', sans-serif;
    box-shadow: 0 20px 50px rgba(0,0,0,0.5);
  }

  /* --- LA LUNE VAPOREUSE --- */
  .moon-container {
    position: absolute;
    top: 30px;
    right: 40px;
    z-index: 1;
  }

  .moon-disk {
    width: 45px;
    height: 45px;
    background: var(--moon-core);
    border-radius: 50%;
    filter: blur(2px);
    box-shadow: 0 0 20px var(--moon-halo), 0 0 40px rgba(200, 220, 255, 0.1);
    animation: moon-pulse 10s infinite alternate ease-in-out;
  }

  /* --- LES ÉTOILES SCINTILLANTES --- */
  .star-container {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .star {
    position: absolute;
    background: var(--star-color);
    border-radius: 50%;
    opacity: 0;
    animation: twinkle var(--duration) infinite ease-in-out;
  }

  /* Tailles d'étoiles pour la parallaxe */
  .star.small { width: 1px; height: 1px; }
  .star.medium { width: 2px; height: 2px; filter: blur(0.5px); }

  @keyframes twinkle {
    0%, 100% { opacity: 0.2; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.2); }
  }

  @keyframes moon-pulse {
    from { opacity: 0.8; transform: scale(1); }
    to { opacity: 1; transform: scale(1.05); }
  }

  /* --- INTERFACE --- */
  .ui-content {
    position: relative; z-index: 10;
    color: var(--text-main); text-align: center; padding-top: 50px;
  }
  .temp { font-size: 72px; font-weight: 900; margin: 0; letter-spacing: -3px; }
  .condition { font-size: 20px; opacity: 0.7; margin-top: -5px; }
</style>
</head>
<body>

<div class="widget-preview">
  <div class="moon-container">
    <div class="moon-disk"></div>
  </div>

  <div class="star-container">
    <div class="star small" style="top: 20%; left: 15%; --duration: 3s;"></div>
    <div class="star medium" style="top: 10%; left: 40%; --duration: 4s;"></div>
    <div class="star small" style="top: 40%; left: 80%; --duration: 5s;"></div>
    <div class="star medium" style="top: 60%; left: 10%; --duration: 3.5s;"></div>
    <div class="star small" style="top: 75%; left: 30%; --duration: 6s;"></div>
    <div class="star medium" style="top: 85%; left: 70%; --duration: 4.5s;"></div>
    <div class="star small" style="top: 25%; left: 65%; --duration: 3.2s;"></div>
    <div class="star small" style="top: 55%; left: 90%; --duration: 4.8s;"></div>
  </div>

  <div class="ui-content">
    <p class="temp">14°C</p>
    <p class="condition">Nuit Claire</p>
  </div>
</div>

</body>
</html>
